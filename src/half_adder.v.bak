module half_adder(input [3:0]a, input [3:0]b, output [3:0]sum, output carry);
	
	xor saida_da_soma(sum, a, b);
	and saida_do_carry(carry, a, b);
	
	//assign sum = a ^ b;
	//assign carry = a & b;
endmodule
